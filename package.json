{
  "name": "grok-codex-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Local Responses API adapter from Codex to the subscription-backed Grok CLI chat proxy",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install:macos": "node scripts/install-macos.mjs --make-default",
    "start": "node src/server.js",
    "test": "node --test"
  }
}
